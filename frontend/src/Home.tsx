import Loading from "./heartForArt.png";

const Home = () => {
  return (
    <img
      src={Loading}
      style={{ height: "100%", width: "100%", zIndex: 1002 }}
    />
  );
};

export default Home;
